diff -Naur output/build/uclibc-org/Makefile.in output/build/uclibc-0.9.30.1/Makefile.in
--- a/Makefile.in	2017-10-12 18:26:36.539135658 +0200
+++ b/Makefile.in	2017-10-12 19:01:29.738562904 +0200
@@ -192,6 +192,8 @@
 
 RUNTIME_PREFIX_LIB_FROM_DEVEL_PREFIX_LIB=$(shell $(top_srcdir)extra/scripts/relative_path.sh $(DEVEL_PREFIX)lib $(RUNTIME_PREFIX)lib)
 
+startfiles: $(crt-y)
+
 $(top_builddir)extra/scripts/unifdef: $(top_srcdir)extra/scripts/unifdef.c
 	$(hcompile.u)
 
@@ -368,8 +370,12 @@
 	$(RM) $(PREFIX)$(DEVEL_PREFIX)include/ifaddrs.h
 endif
 
+# Installs startfiles
+install_startfiles: startfiles | $(PREFIX)$(DEVEL_PREFIX)lib/
+	-$(INSTALL) -m 644 $(startfiles) $(PREFIX)$(DEVEL_PREFIX)lib/
+
 # Installs development library links.
-install_dev: install_headers all
+install_dev: install_headers install_startfiles all
 	$(INSTALL) -d $(PREFIX)$(DEVEL_PREFIX)lib
 	-$(INSTALL) -m 644 lib/*.[ao] $(PREFIX)$(DEVEL_PREFIX)lib/
 ifeq ($(HAVE_SHARED),y)
diff -Naur output/build/uclibc-org/Makerules output/build/uclibc-0.9.30.1/Makerules
--- a/Makerules	2017-10-12 18:26:36.559134700 +0200
+++ b/Makerules	2017-10-12 18:58:21.979446668 +0200
@@ -386,7 +386,9 @@
 CRTS_COMPAT :=
 #endif
 
-$(crt-y): $(CRTS) $(CTOR_TARGETS) $(CRTS_COMPAT) $(CRTRELOC)
+startfiles = $(CRTS) $(CTOR_TARGETS) $(CRTS_COMPAT) $(CRTRELOC)
+$(crt-y): $(startfiles)
+
 $(CRTS) $(CTOR_TARGETS) $(CRTS_COMPAT) $(CRTRELOC): | headers
 
 $(top_builddir)lib/$(NONSHARED_LIBNAME): $(libc-nonshared-y)
