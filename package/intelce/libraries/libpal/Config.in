config BR2_PACKAGE_LIBPAL
	bool "libpal"
	depends on BR2_TOOLCHAIN_HAS_THREADS
	select BR2_PACKAGE_LIBOSAL
	help
		Intel CE PAL library

comment "libpal requires a toolchain with thread support"
	depends on !BR2_TOOLCHAIN_HAS_THREADS
