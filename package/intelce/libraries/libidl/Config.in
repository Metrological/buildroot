config BR2_PACKAGE_LIBIDL
	bool "libidl"
	depends on BR2_TOOLCHAIN_HAS_THREADS
	select BR2_PACKAGE_LIBOSAL
	help
		Intel CE IDL library

comment "libidl requires a toolchain with thread support"
	depends on !BR2_TOOLCHAIN_HAS_THREADS
